# บทเรียนจากสิ่งที่เคยพัง

ไฟล์นี้เก็บ **ปัญหาที่เกิดขึ้นจริงในโปรเจกต์นี้** ไม่ใช่ทฤษฎีทั่วไป ทุกบทอ้างอิง commit จริงที่ตรวจสอบย้อนหลังได้

**เขียนเพิ่มเมื่อไหร่:** หลังแก้ปัญหาใหญ่ทุกครั้ง โดยเฉพาะปัญหาที่ใช้เวลาหาสาเหตุนาน
**อ่านเมื่อไหร่:** ก่อนแตะส่วนที่เกี่ยวข้อง และก่อน deploy

---

## 1. ค่า config ที่แก้จาก dashboard หายไปเงียบ ๆ

**commit:** `fc59089` — 2026-08-03

**เกิดอะไรขึ้น:** มีการแก้ค่า `ALLOWED_ORIGIN` (รายชื่อเว็บที่ได้รับอนุญาตให้เรียก API) ผ่านหน้าเว็บ Cloudflare dashboard ให้เหลือแค่ `rubriclensai.com` ซึ่งเป็นโดเมนที่ยัง**ไม่ได้ต่อ DNS** ผลคือเว็บจริงที่คนใช้อยู่ (`rubriclensai.pages.dev`) ถูกเบราว์เซอร์บล็อกทันที ปุ่มเริ่มตรวจขึ้นข้อความดิบ ๆ ว่า `Failed to fetch`

**ทำไมถึงหายาก:** ค่าที่แก้จาก dashboard **ไม่ถูกบันทึกลง git** เปิดโค้ดดูก็เห็นค่าที่ถูกต้อง แต่ของจริงบนเซิร์ฟเวอร์เป็นอีกค่าหนึ่ง และข้อความ `Failed to fetch` ของเบราว์เซอร์ไม่ได้บอกว่าเป็นเรื่อง CORS เลย

**กฎที่ได้:**
- `wrangler.jsonc` คือแหล่งความจริงเดียวของ config ทั้งหมด **ห้ามแก้ค่า production ผ่าน dashboard เด็ดขาด**
- เวลาจะเปลี่ยนโดเมน ให้ใส่โดเมนใหม่**เพิ่ม** ไม่ใช่แทนที่ของเดิม จนกว่าโดเมนใหม่จะใช้งานได้จริง
- เจอ `Failed to fetch` ให้สงสัย CORS เป็นอันดับแรก

---

## 2. Gemini 3 คิดเพลินจนตอบไม่ทัน

**commit:** `1cfc762` — 2026-08-02

**เกิดอะไรขึ้น:** หลังย้ายมาใช้ Gemini 3 การตรวจเอกสารยาวเริ่มค้างจนหมดเวลา (timeout) สาเหตุคือ Gemini 3 ใช้ "โควตาคำตอบ" (`maxOutputTokens`) ร่วมกันระหว่าง**ความคิดภายในของโมเดล**กับ**คำตอบ JSON ที่ส่งกลับมาจริง** พอโมเดลคิดเยอะ โควตาก็หมดก่อนที่จะเขียน JSON เสร็จ

**แก้ยังไง:** ตั้ง `thinkingConfig: { thinkingLevel: 'low' }` เพราะการให้คะแนนตาม rubric เป็นงานทำตามคำสั่งที่มีกรอบชัดอยู่แล้ว ไม่ต้องคิดลึก แล้วขยายโควตาจาก `500 + (หัวข้อ × 250)` เป็น `1,000 + (หัวข้อ × 500)` เผื่อ JSON ภาษาไทยที่กินที่มากกว่าภาษาอังกฤษ

**การป้องกันเพิ่ม:** commit `0f5393a` — 2026-08-08 เพิ่มเพดานฝั่ง Worker แทนการพึ่ง timeout ของ browser อย่างเดียว การวิเคราะห์หนึ่งคำขอมีเพดานรวม 100 วินาที, `countTokens` มีเพดาน 10 วินาที และ model request มีเพดาน 60 วินาที สัญญาณยกเลิกชุดเดียวถูกส่งผ่าน primary model, fallback, retry, chunk และ consolidation โดย `wrangler.jsonc` เปิด `enable_request_signal` ให้ Worker รับการยกเลิกจาก browser ได้

**แก้เพิ่มรอบนี้:** การส่ง abort signal ให้ SDK อย่างเดียวยังไม่ใช่เพดานเวลารอ เพราะ SDK อาจ retry ต่อและปล่อย promise ค้าง จึงเพิ่ม application-level wait boundary รอบทุก `countTokens`/`generateContent` ที่ 10/60 วินาที และเริ่มนาฬิการวม 100 วินาทีตั้งแต่เข้า `handleAnalyze` ก่อน validation/KV/provider preparation timeout ราย call ยังใช้ fallback เดิมได้ตราบใดที่เวลารวมยังเหลือ แต่ deadline รวม/cancel จะไม่เริ่ม provider call ถัดไป; health probe 5 วินาทีก็ใช้หลักเดียวกัน

เหตุผลที่ต้องมีหลายชั้นคือการที่ browser หยุดรอไม่ได้แปลว่า Worker และ Gemini หยุดเองโดยอัตโนมัติ ถ้าไม่มี deadline ฝั่ง Worker งานอาจทำต่อหลังผู้ใช้เห็น timeout แล้วและยังกินโควตา ส่วนการ abort ที่ส่งถึง SDK ก็รับประกันได้เพียงว่าฝั่ง client/Worker หยุดรอ ผู้ให้บริการอาจทำงานที่รับไปแล้วต่อและคิดค่าใช้จ่ายได้ จึงห้ามอ้างว่า “ผู้ใช้กดยกเลิก = billing ถูกยกเลิกแน่นอน”

**กฎที่ได้:**
- เปลี่ยนรุ่นโมเดลทุกครั้ง ต้องทดสอบด้วย**เอกสารยาวที่สุดที่ระบบรับได้** ไม่ใช่เอกสารสั้น ๆ
- ทุกครั้งที่เรียกโมเดล ต้องมีเพดานเวลา **ฝั่งที่เรียก model จริง** อย่าปล่อยให้ browser timeout เป็นด่านเดียว
- retry และ fallback ต้องใช้ deadline รวมเดียวกัน ไม่เริ่มนาฬิกาใหม่จนเวลารวมยืดออกไปเรื่อย ๆ
- สัญญาณยกเลิกต้องผ่านทุกเส้นทาง รวม `countTokens`, retry แก้ JSON/ภาษา, chunk และ consolidation; ขาดเพียงทางเดียวก็ยังมีงานเบื้องหลังค้างได้
- deadline ป้องกันเวลารอและลดความเสี่ยงค่าใช้จ่าย แต่ budget guard กับ monitoring ยังจำเป็น เพราะ abort ไม่ใช่หลักฐานว่าผู้ให้บริการหยุดคิดเงินแล้ว

---

## 3. จับ error รวมกันทีเดียว ทำให้แยกไม่ออกว่าพังเพราะอะไร

**commit:** `8362418` — 2026-08-01

**เกิดอะไรขึ้น:** โค้ดเดิมครอบทั้ง "การเรียก Gemini" และ "การแปลงคำตอบเป็น JSON" ไว้ใน `try` ก้อนเดียว ผลคือแยกไม่ออกว่าที่พังเป็นเพราะ **ต่อ Gemini ไม่ได้** หรือ **Gemini ตอบมาผิดรูป** ระบบเลยไปลองเรียกใหม่พร้อมข้อความ "ขอ JSON เท่านั้น" ทั้งที่ปัญหาจริงคือต่อไม่ติด = เสียเวลาและเสียเงินเรียกซ้ำฟรี ๆ

**แก้ยังไง:** แยกเป็นสองขั้น — ถ้าเรียกไม่ติดคืน `502` พร้อมข้อความว่าเชื่อมต่อไม่ได้ทันที ส่วนการแปลง JSON แยกออกมาเป็นฟังก์ชัน `safelyParseJson` ที่คืน `undefined` แทนที่จะโยน error

**กฎที่ได้:** `try/catch` หนึ่งก้อน ควรครอบ **ความล้มเหลวแบบเดียว** ถ้าในนั้นมีสองอย่างที่พังได้คนละสาเหตุ ให้แยกก้อน

---

## 4. นับ token ผิดเพราะลืมนับคำสั่งระบบ

**commit:** `682cec2` — 2026-08-01

**เกิดอะไรขึ้น:** โค้ดกันงบประมาณเรียก `countTokens` โดยส่ง `systemInstruction` (คำสั่งประจำที่บอกโมเดลว่าต้องทำตัวยังไง) ไปทาง `config` ซึ่งฝั่ง API **ไม่ได้เอามานับ** ผลคือระบบคิดว่าใช้ token น้อยกว่าความจริงทุกครั้ง — ด่านกัน 120,000 token จึงปล่อยเอกสารที่ใหญ่เกินจริงผ่านไปได้

**แก้ยังไง:** ต่อ `systemInstruction` เข้ากับ prompt เป็นข้อความเดียวก่อนนับ

**กฎที่ได้:** ตัวเลขที่ใช้เป็น "ด่านกัน" ต้องพิสูจน์ว่านับครบจริง อย่าเชื่อว่า API นับให้เหมือนที่เราคิด — ด่านที่นับผิดอันตรายกว่าไม่มีด่าน เพราะทำให้สบายใจผิด ๆ

---

## 5. เว็บ production รันด้วยข้อมูลตัวอย่างโดยไม่มีใครรู้

**commit:** `93f3c10` — 2026-08-01

**เกิดอะไรขึ้น:** โค้ดเดิมเช็คว่า `VITE_USE_MOCK_ANALYSIS !== 'false'` ถึงจะใช้ AI จริง แปลว่าถ้าตอน build **ลืมตั้งค่านี้** (ซึ่งเป็นค่าว่าง ไม่ใช่ `'false'`) เว็บ production จะใช้ **ข้อมูลตัวอย่างปลอม** แทน AI จริง โดยหน้าเว็บดูปกติทุกอย่าง

**แก้ยังไง:** เขียนฟังก์ชัน `usesMockAnalysis()` และ `getApiBaseUrl()` ที่มีค่าเริ่มต้นชัดเจน — ตอน dev ใช้ mock, ตอน production ใช้ของจริง — แล้วเอา comment ครอบค่าใน `.env.example` เพื่อให้ค่าเริ่มต้นทำงาน

**กฎที่ได้:**
- ค่าเริ่มต้นเมื่อ "ลืมตั้งค่า" ต้องเป็นค่าที่**ปลอดภัยและถูกต้อง** ไม่ใช่ค่าที่ทำให้ระบบทำงานผิดแบบเงียบ ๆ
- ปุ่มบนหน้าเว็บควรบอกเสมอว่ากำลังจะตรวจด้วย "ข้อมูลตัวอย่าง" หรือ "AI" — ตอนนี้บอกแล้ว อย่าเอาออก

---

## 6. Health check บอกว่า "ปกติ" ทั้งที่ตรวจเอกสารไม่ได้เลย

**commit:** บทนี้ — 2026-08-05

**เกิดอะไรขึ้น:** Gemini API key หมดอายุ/ถูกลบ ผลคือทุกคำขอตรวจเอกสารตอบ `503 AI_CONFIGURATION` แต่ `/api/health` ยังตอบ `"status":"ok"` และ `"aiConfigured":true` ตามปกติ เพราะโค้ดเช็คแค่ `Boolean(env.GEMINI_API_KEY)` — คือเช็คว่า **มีค่าอยู่ไหม** ไม่ได้เช็คว่า **ใช้ได้ไหม** key ที่ถูกลบไปแล้วก็ยังเป็นข้อความยาว ๆ ที่ไม่ว่างเปล่าอยู่ดี

**ทำไมถึงหายาก:** หน้า health เป็นเครื่องมือชิ้นเดียวที่เจ้าของโปรเจกต์ใช้ดูว่าระบบปกติไหม พอมันบอกว่าปกติ ก็ไม่มีใครสงสัย กว่าจะรู้ว่าพังคือตอนที่ผู้ใช้ลองตรวจเอกสารแล้วขึ้น "กรุณาแจ้งผู้ดูแลระบบ" — วิธีเดียวที่ยืนยันได้จริงคือยิง `/api/analyze` ด้วยเอกสารจริงหนึ่งครั้ง

**แก้ยังไง:** เพิ่มโหมด `GET /api/health?verify=ai` ที่เรียก `countTokens` ของ Gemini จริง (เป็น API call ที่ต้องยืนยันตัวตนแต่ไม่กินโควตาคำตอบ) แล้วรายงาน `aiReachable` กับ `aiCheckCode` ถ้า key ใช้ไม่ได้จะตอบ `status: "degraded"` + HTTP 503 ส่วน `/api/health` เปล่า ๆ ยังเร็วและฟรีเหมือนเดิม โหมด verify มีเพดานเวลา 5 วินาที และ cache ผล 5 นาทีเพื่อไม่ให้ endpoint สาธารณะถูกยิงจนกิน rate limit ที่การตรวจจริงต้องใช้

**แก้เพิ่มรอบสอง (วันเดียวกัน):** โหมด verify แก้ได้แค่ "ถามแล้วได้คำตอบจริง" แต่ยังต้องมีคนนึกจะถาม จึงเพิ่ม Cron Trigger ทุกชั่วโมงให้ระบบถามเอง เขียน log เสมอ และส่ง webhook ถ้าตั้ง `ALERT_WEBHOOK_URL` ไว้ พร้อมกับคืน `aiCheckAgeSeconds` เพราะคำตอบที่ cache ไว้ 5 นาทีเคยทำให้เกือบเชื่อคำตัดสินที่ตัดสินไว้ก่อนเปลี่ยน key

**แก้เพิ่มรอบนี้:** เดิม cron เขียน health cache ก่อน log/webhook ถ้า KV ล่ม alert จึงหายทั้งหมด และ webhook ที่ตอบ HTTP 500 ถูกนับเป็นสำเร็จ ตอนนี้ outage ถูก log/alert ก่อนเขียน cache, HTTP non-2xx ถูก log เป็น `gemini_watch_alert_failed`, payload ส่งทั้ง `content` (Discord) กับ `text` (Slack), แล้วค่อยให้ cache error ทำ scheduled run แดง

**กฎที่ได้:**
- ตัวชี้วัดสุขภาพระบบต้องวัด **ผลลัพธ์** ไม่ใช่วัด **การมีอยู่ของค่า config** — "ตั้งค่าไว้แล้ว" กับ "ใช้งานได้จริง" เป็นคนละเรื่อง
- ถ้าเขียน health check ที่ไม่ได้แตะระบบภายนอกจริง ต้องตั้งชื่อฟิลด์ให้ตรงกับสิ่งที่มันวัดได้จริง และมีอีกทางให้ตรวจของจริงเสมอ
- **ตัวชี้วัดที่ต้องรอให้คนมากดถาม ไม่ใช่ตัวชี้วัด** — ถ้าไม่มีอะไรถามเองเป็นระยะ ก็ยังต้องรอผู้ใช้เจอปัญหาก่อนอยู่ดี
- ค่าที่ cache ไว้ต้องบอกอายุของตัวเองเสมอ ไม่งั้นคนอ่านแยกไม่ออกว่ากำลังดูของสดหรือของเก่า
- เจอ `AI_CONFIGURATION` ให้สงสัย key ก่อน ไม่ใช่โควตา — โควตาเต็มจะขึ้น `GEMINI_DAILY_QUOTA` คนละรหัสกัน

---

## 7. อยากรู้ว่าดีขึ้นไหม แต่ไม่ได้วัดไว้ตั้งแต่แรก

**commit:** บทนี้ — 2026-08-05

**เกิดอะไรขึ้น:** เจอว่าโมเดลแทรกตัวอักษรญี่ปุ่นลงกลางประโยคไทย (`ผู้ตรวจ評価ควรตรวจสอบ`) จึงแก้ถ้อยคำใน prompt ให้ห้ามชัดเจน แล้วก็ตอบคำถามง่ายๆ ไม่ได้ว่า **"แก้แล้วดีขึ้นจริงไหม"** เพราะไม่มีตัวเลขทั้งก่อนและหลัง มีแค่ตัวอย่างเดียวที่ดูดีขึ้น ซึ่งพิสูจน์อะไรไม่ได้เลยกับระบบที่ตอบไม่เหมือนเดิมทุกครั้ง

**ทำไมถึงพลาด:** คิดว่า log พอ แต่ `observability.head_sampling_rate` ตั้งไว้ `0.1` แปลว่าเก็บ log แค่ 10% ของ request เหตุการณ์ที่เกิดนานๆ ครั้งจึงหายไปกับการสุ่มเกือบหมด

**แก้ยังไง:** นับลง KV ทุกครั้งที่เกิดจริง (`stats:foreign-script-retries:วันที่` และ `stats:foreign-script-persisted:วันที่` อายุ 36 ชั่วโมง) แล้วเปิดให้อ่านที่ `/api/health?verify=ai` การนับถูกครอบ try/catch เพราะ **การเก็บสถิติล้มเหลว ห้ามทำให้การตรวจเอกสารของผู้ใช้ล้มเหลว**

**กฎที่ได้:**
- เวลาจะแก้พฤติกรรมที่วัดไม่ได้ ให้ **ใส่เครื่องวัดพร้อมกับการแก้เสมอ** ไม่งั้นอีกเดือนก็ยังตอบไม่ได้ว่าดีขึ้นไหม
- log ที่ถูกสุ่มเก็บ ใช้วัดของที่เกิดบ่อยได้ แต่ใช้วัดของที่เกิดนานๆ ครั้งไม่ได้ ถ้าต้องนับให้ครบต้องนับลงที่เก็บถาวร
- "หนึ่งตัวอย่างดูดีขึ้น" ไม่ใช่หลักฐาน — พูดให้ตรงว่ายังไม่รู้ ดีกว่าบอกว่าแก้แล้ว

---

## 8. ตัวนับที่อ่าน `null` แล้วเขียน `1` อาจลดยอดจริงลง ไม่ใช่แค่นับตกหนึ่งครั้ง

**แก้ใน local follow-up — 2026-08-09 (ยังไม่ได้ push/deploy)**

**เกิดอะไรขึ้น:** ตัวนับ KV เดิมทำ `get → ถ้า null ถือเป็น 0 → put(1)` ทั้ง rate limit, daily request, token budget และสถิติภาษา แต่ Cloudflare KV cache ผลว่า “ไม่มี key” ได้ และ concurrent write เป็น last-write-wins ดังนั้น edge ที่เห็น `null` เก่าสามารถเขียน `1` ทับยอดจริงที่สูงกว่าได้ ด่านค่าใช้จ่ายจึงอาจนับต่ำแบบเงียบ ๆ

**แก้ยังไง:** เลิกเขียนยอดรวม key เดิม แต่เขียนหนึ่งเหตุการณ์ต่อหนึ่ง key ที่มี UUID และ TTL แล้วรวม `metadata.amount` ผ่าน `list()` ตอนตรวจเพดาน ค่า aggregate รุ่นเก่ายังถูกอ่านมารวมระหว่าง migration แต่ไม่ถูกเขียนอีก จึงไม่มี stale/null read ไปทำให้ยอดที่บันทึกไว้ลดลง เพิ่ม test จำลอง store ที่คืน `null` เก่าสำหรับ rate, daily request, token และ quality events โดยตรง

**ข้อจำกัดที่ต้องพูดตรง ๆ:** KV ยังเป็น eventual consistency รายการจากอีกภูมิภาคอาจยังไม่ปรากฏในทันที คำขอพร้อมกันจึงยังมีโอกาสผ่านเกินเพดานชั่วคราว วิธีนี้ปิด **overwrite** แต่ไม่ได้เปลี่ยน KV ให้เป็นตัวนับ atomic หรือยอด billing จริง เพดานค่าใช้จ่ายแบบแข็งต้องมี provider quota/budget alert หรือกลไกประสานการเขียนที่ได้รับอนุมัติแยกต่างหาก

**กฎที่ได้:**
- ที่เก็บข้อมูล eventual-consistent ห้ามทำ read-modify-write บน counter key เดียวแล้วเรียกว่า “นับครบ”
- test ของด่านค่าใช้จ่ายต้องจำลอง stale `null` และ concurrent visibility ไม่ใช่ทดสอบแค่ Map ที่อ่านค่าล่าสุดเสมอ
- แยกคำว่า “กัน overwrite”, “rate-abuse guard” และ “hard billing ceiling” ให้ชัด เพราะเป็นหลักประกันคนละระดับ

---

## สรุปสิ่งที่ 8 บทนี้มีร่วมกัน

ทุกบทข้างบนคือ **ความผิดพลาดที่ระบบไม่ร้อง** ระบบยังทำงานต่อ ไม่มีข้อความ error สีแดง แต่ผลลัพธ์ผิด นี่คือประเภทของบั๊กที่โปรเจกต์นี้ต้องระวังที่สุด และเป็นเหตุผลที่กติกาใน `AGENTS.md` ยืนยันว่าคำว่า "เสร็จแล้ว" ต้องมาคู่กับผล test ดิบเสมอ
